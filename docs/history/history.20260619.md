# 작업 이력 — 2026-06-19

**한 줄 요약**: 에이전트 조직을 전역(중앙)으로 일원화하고, WBS를 분석·기획·설계 단계로 구성(담당자는 전역 에이전트 이름으로 표기)했으며, WBS 상단 떨림 버그를 수정하고 프로젝트 일정을 6/17~12/31로 고정했다.

---

## 1. 에이전트 조직 중앙화

공통 역할 에이전트를 프로젝트 로컬(`.claude/agents/`)에서 **전역 `~/.claude/agents/`로 일원화**하고, 중복되던 로컬 정의를 제거했다(`9af0eaf`).

- 이제 모든 에이전트는 전역 조직에서 로드된다. 전역 정의에는 `<!-- dotype:identity --> **이름:** … · **역할:** …` 형태로 **인격명**이 부여돼 있다(예: 최기획=기획자, 도아컨=아키텍트, 한서기=서비스 기획자).

## 2. WBS — 분석·기획·설계 단계 구성

라이브 D1 `wbs_item`이 비어 있어 간트가 비어 있던 것을, 어제까지(6/17~18) 수행한 작업으로 채웠다(`7fb0a70`).

- 3단계 17작업(모두 완료 100%): **분석**(경쟁분석·포지셔닝) → **기획**(서비스/도메인 기획 00·02~08·11·12) → **설계**(아키텍처·ERD·IA·교수설계 품질·에이전트 시스템).
- 9단계 배포 로드맵은 `docs/planning/11-ROADMAP-WBS.md`·대시보드(현황판)에 그대로 유지.

## 3. WBS 담당자 = 전역 에이전트 이름

작업 담당자를 전역 에이전트로 정확히 재배정하고(`e2d3a2b`), 역할 슬러그 대신 **지정 인격명으로 표기**하도록 변경했다(`fba3a3e`).

- 한서기(service-planner) · 최기획(planner) · 문프매(pms-developer) · 도아컨(architect) · 한데관(dba) · 서디자(ux-designer) · 윤하람(instructional-designer) · 서지원(growth-keeper).
- `wbsData.ts`·`seed.sql`·`wbs.vue`(PEOPLE/PCOLOR) 동기화.

## 4. WBS 상단 떨림 버그 수정

스크롤 시 상단(GNB·KPI)이 진동하던 버그를 근본 원인까지 수정했다(`2f4f42f`).

- **원인**: 크롬 접/펴기가 `.gantt` 스크롤 컨테이너 높이를 바꿔 `scrollTop`이 트랜지션 중 보정되고, 그 보정이 `@scroll`을 재발화해 민감한 양방향 토글을 뒤집는 **피드백 루프**(짧은 내용에서 특히 심함).
- **수정**: 토글 직후 300ms 스크롤 이벤트 잠금 + 접어도 스크롤 여지가 남을 때만 접기 + 최상단/위로 스크롤 시 펴기.

## 5. 프로젝트 일정 고정 (6/17 ~ 12/31)

간트 타임라인을 작업 날짜 min/max가 아니라 **프로젝트 기간(2026-06-17 ~ 2026-12-31) 고정**으로 변경했다(`06d5bb4`). 작업이 범위를 벗어나면 자동 확장. 상단 부제에 기간 표시.

## 6. 산출물

- 프로덕션 반영: https://malgn-studio-mng.pages.dev/wbs (분석·기획·설계 3단계, 담당 인격명, 6~12월 타임라인).
- 변경 파일: `app/pages/wbs.vue` · `app/utils/wbsData.ts` · `server/db/seed.sql`.
- 커밋: `9af0eaf`·`7fb0a70`·`e2d3a2b`·`fba3a3e`·`2f4f42f`·`06d5bb4` (+ 본 이력).

## 7. 다음 단계

- 원하면 9단계 배포 로드맵을 7~12월 일정으로 간트에 배치(현재 이후 구간 비어 있음) 또는 주(week) 단위 보기로 폭 축소.
- 형제 레포 P0 착수(`malgn-studio`·`-api`·`-admin`·`-lms`).
- WBS 떨림 수정 실사용 확인(레이아웃 자동 테스트 불가 → 육안).
</content>
